# help

officekit keeps command help in markdown so the CLI can render docs without hardcoding every explanation in the command parser.

## Goals

- keep docs editable without recompiling command code
- preserve helpful OfficeCLI-style operator guidance
- make lineage and migration caveats visible near the help surface
